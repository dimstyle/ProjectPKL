{
  "name": "akadub",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "mock:lint": "npx spectral lint api/openapi.yaml"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.16.0",
    "concurrently": "^10.0.3"
  }
}
